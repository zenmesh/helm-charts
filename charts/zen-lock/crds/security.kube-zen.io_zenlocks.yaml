---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: zenlocks.security.kube-zen.io
spec:
  group: security.kube-zen.io
  names:
    kind: ZenLock
    listKind: ZenLockList
    plural: zenlocks
    shortNames:
    - zl
    - zenlock
    singular: zenlock
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZenLock is the Schema for the zenlocks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZenLockSpec defines the desired state of ZenLock
            properties:
              algorithm:
                default: age
                description: 'Algorithm specifies the encryption method (default:
                  "age")'
                enum:
                - age
                type: string
              allowedSubjects:
                description: |-
                  AllowedSubjects is an optional list of ServiceAccounts allowed to use this secret
                  Currently only ServiceAccount kind is supported. User and Group kinds are not yet implemented.
                items:
                  description: |-
                    SubjectReference references a Kubernetes subject
                    Currently only ServiceAccount kind is supported. User and Group kinds are planned for future releases.
                  properties:
                    kind:
                      description: Kind is the kind of subject. Currently only "ServiceAccount"
                        is supported.
                      enum:
                      - ServiceAccount
                      type: string
                    name:
                      description: Name is the name of the subject
                      type: string
                    namespace:
                      description: Namespace is the namespace of the subject (required
                        for ServiceAccount)
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                type: array
              encryptedData:
                additionalProperties:
                  type: string
                description: EncryptedData is a map of key -> Base64-encoded ciphertext
                type: object
            required:
            - encryptedData
            type: object
          status:
            description: ZenLockStatus defines the observed state of ZenLock
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the ZenLock's state
                items:
                  description: ZenLockCondition describes the state of a ZenLock at
                    a certain point
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable explanation of the
                        condition
                      type: string
                    reason:
                      description: Reason for the condition's last transition
                      type: string
                    status:
                      description: Status of the condition (True, False, Unknown)
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              lastRotation:
                description: LastRotation is the timestamp of the last key rotation
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the ZenLock
                enum:
                - Ready
                - Error
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
