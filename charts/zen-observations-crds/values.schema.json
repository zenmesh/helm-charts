{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "zen-observations-crds Helm Chart Values",
  "description": "Configuration values for zen-observations-crds chart",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Whether to install the CRDs. Default: false (CRD-free unless explicitly enabled)",
      "default": false
    },
    "namespace": {
      "type": "string",
      "description": "Namespace where CRDs will be installed",
      "default": "default"
    },
    "labels": {
      "type": "object",
      "description": "Labels to apply to CRD resources",
      "additionalProperties": {
        "type": "string"
      }
    },
    "annotations": {
      "type": "object",
      "description": "Annotations to apply to CRD resources",
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "required": ["enabled"]
}

