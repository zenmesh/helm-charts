{{- if .Values.crds.enabled }}
{{- $files := .Files }}
{{- $crdPath := "files/crds/observation_crd.yaml" }}
{{- if $files.Get $crdPath }}
{{- $files.Get $crdPath | nindent 0 }}
{{- else }}
# CRD file not found in chart files
# Please ensure observation_crd.yaml is included in the chart's files/crds/ directory
{{- end }}
{{- end }}
